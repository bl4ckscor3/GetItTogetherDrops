buildscript {
    repositories {
        maven { url = 'https://files.minecraftforge.net/maven' }
        jcenter()
        mavenCentral()
        maven {url='https://dist.creeper.host/Sponge/maven'}

    }
    dependencies {
        classpath group: 'net.minecraftforge.gradle', name: 'ForgeGradle', version: '3.+', changing: true
        classpath 'org.spongepowered:mixingradle:0.7-SNAPSHOT'
    }
}
apply plugin: 'net.minecraftforge.gradle'
apply plugin: 'eclipse'
apply plugin: 'maven-publish'

version = "1.15.2-v1.0"
group = "bl4ckscor3.mod.getittogetherdrops"
archivesBaseName = "getittogetherdrops"

sourceCompatibility = targetCompatibility = compileJava.sourceCompatibility = compileJava.targetCompatibility = '1.8'

minecraft {
    mappings channel: "snapshot", version: "20201003-1.15.1"

    accessTransformer = file('src/main/resources/META-INF/accesstransformer.cfg')

    runs {
        client {
            workingDirectory project.file('run')
            arg "-mixin.config=" + archivesBaseName + ".mixins.json"
            property 'forge.logging.console.level', 'debug'

            mods {
                getittogetherdrops {
                    source sourceSets.main
                }
            }
        }

        server {
            workingDirectory project.file('run')
            arg "-mixin.config=" + archivesBaseName + ".mixins.json"
            property 'forge.logging.console.level', 'debug'

            mods {
                getittogetherdrops {
                    source sourceSets.main
                }
            }
        }
    }
}

repositories {
    maven { url = 'https://dist.creeper.host/Sponge/maven' }
    jcenter()
    mavenCentral()
}

dependencies {
    minecraft "net.minecraftforge:forge:1.15.2-31.2.43"
    compile 'org.spongepowered:mixin:0.8'
}

jar {
    manifest {
        attributes([
                "Specification-Title": "getittogetherdrops",
                "Specification-Vendor": "bl4ckscor3",
                "Specification-Version": "v1.0",
                "Implementation-Title": "Get It Together, Drops!",
                "Implementation-Version": "v1.0",
                "Implementation-Vendor": "getittogetherdrops",
                "Implementation-Timestamp": new Date().format("yyyy-MM-dd'T'HH:mm:ssZ"),
                "MixinConfigs": "getittogetherdrops.mixins.json"
        ])
    }
}

apply plugin: 'org.spongepowered.mixin'

mixin {
    add sourceSets.main, "getittogetherdrops.refmap.json"
}